# Representing points

"""
We use a list, where each item in the list represents a dimension of the point.

For example: the point (5, 8) could be represented in Python like this:
pt1 = [5, 8]

*Note* We can only find the distance between the two points if they have the same number of dimensions!

"""

two_dimension_point = [10, 2]
four_dimension_point = [1, 2, 3, 4]
five_dimension_point = [30, -1, 50, 0, 2]

