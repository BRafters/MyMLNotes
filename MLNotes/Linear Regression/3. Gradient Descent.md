As we try to minimize **Loss**, we take each parameter we are changing, and move it as long as it decreases **Loss**
- For instance, it’s like we’re moving down a hill, and stop once we reach the bottom
- The process by which we do this is called **gradient descent**
    - We move in the direction that decreases our loss the most
- Gradient refers to the slope of the curve at any point
￼![[loss_curve.svg]]- For example, we’re trying to find the intercept of a line
    - We have a guess of 10 for the intercept
    - At the point of 10 on the curve, the slope is downward
        - If we increase the intercept, we should be lowering the loss
    - We continue following the gradient downward 
￼![[Loss vs b value.gif]]
- To find gradient of Loss as intercept changes, the formula comes out to be
![[Pasted Graphic 1.png]]￼
    - N is the number of datapoints we have in our dataset
    - m is the current gradient guess
    - b is the current intercept guess
- Basically:
    - We find the sum of y_value -  (m * x_value + b) for all the y_values and x_values we have
    - Then we multiply the sum by a factor of -2/N

### Gradient Descent for Slope

To find the m gradient, or the way loss changes as the slope of our line changes, we can use this formula:
￼![[Pasted Graphic 2.png]]
- N = number of datapoints in dataset
- m = current gradient guess
- b = current intercept guess

To find the m gradient:
- We find the sum of x_value * (y_value - (m * x_value + b)) for all x and y values
- Multiply the sum by a factor of  -2/N

### Putting it all together

Now that we know how to calculate the gradient and slope, we want to take a step in calculating the gradient
- We do need to think whether a step is too big or too small
    - We don’t want to overshoot the minimum error
- We can scale the size of a step by multiplying the gradient by a learning rate
- To find a new b value, we would say
    - new_b = current_b - (learning_rate * b_gradient)
    - current_b  = current b value guess
    - b_gradient is the gradient of the loss curve at our current guess
    - learning_rate is proportional to the size of the step we want to take 
### Convergence

How do we know when we should stop changing the parameters m and b?
- To answer this, we use convergence
- Convergence is when the loss stops changing or changes very slowly
### Learning Rate

We want our program to be able to iteratively learn what the best m and b values are
- For each m and b pair that we guess, we want to move them in the direction of the gradients we’ve calculated
    - But, how far do we move in that direction?
- We have to choose a learning rate, which will determine how far down the loss curve we go
- A small learning rate will take a long time to converge. You might run out of cycles or time before getting an answer
- A large learning rate might skip over the best value and may not even converge!
- Finding the absolute best learning rate is not necessary for training a model
    - You just have to find a learning rate large enough that gradient descent converges with the efficiency you need, and not so large that convergence never happens
