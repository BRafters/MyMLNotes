- **Supervised Learning** is where data is labeled and the program learns to predict the output from the input data
    - For example, a **supervised** algorithm for credit fraud detection would take as input a set of recorded transactions
    - For each transaction, the program would predict if it is fraudulent or not
- Generally, supervised learning problems can be further grouped into regression and classification problems
- **Supervised learning** works like a teacher or supervisor guiding the machine.
    - We train the machine using the labelled data (correct answers or classifications) which means each input has the correct output in the form of answer or category attached to it
- Another example would be a labeled dataset of images of Elephant, Camel, or Cow would have each image tagged with either “Elephant”, “Camel”, or “Cow”
### Applications of Supervised Learning

- Image Classification
    - It can automatically classify images into different categories such as animals, objects, or scenes
    - Helps in the tasks like image search, content moderation and image based product recommendations
- Medical Diagnosis
    - It can assist in medical diagnosis by analyzing patient data such as medical images, test results, and patient history to identify patterns that suggest specific diseases or conditions
- Fraud detection
    - They can analyze financial transactions and identify patterns that shows fraudulent activity which helps financial institutions prevent fraud and protect their customers
- Natural Language processing (NLP)
    - It plays an important role in NLP tasks including sentiment analysis, machine translation, and text summarization, which enables machines to understand and process human language effectively
##### Regression:
- In regression problems, we are trying to predict a continuous-valued output, such as:
    - What is the housing price in New York
    - What is the value of cryptocurrencies
    - Regression algorithms learn how to connect input data to a specific number or value.
- Some examples of regression algorithms include
    - Linear Regression
    - Polynomial Regression
    - Lasso Regression
    - Ridge Regression
##### Classification:
- In classification problems, we are trying to predict a discrete number of values
- A classification is used to predict categorical values, such as:
    - Whether a customer will buy or not
    - Whether a medical image shows a tumour or not
    - Is this a picture of a human, or a picture of AI?
    - Is this email spam?
- **Classification** algorithms learn how to connect input data to the probability of belonging to different groups or categories
- Some of the most common classification algorithms include:
    - Logistic Regression
    - Support Vector Machines
    - Decision Trees
    - Random Forests
    - Naive Baye
##### Some Examples:
- Thinking about different genres in music.
- Your music teacher plays you songs of the following genres multiple times
    - Indie Rock
    - K-Pop
    - Techno
- The next time you’re listening to the radio, and you hear techno, you may think “this is similar to the 5 techno tracks I learned in class today. This must be Techno!”
- Even though the teacher didn’t tell you about this techno track, she gave you enough examples to run off of, so you can recognize more examples of it
- When we explicitly tell a program what we expect the output to be, and let it learn the rules that produce expected outputs from given inputs, we are performing supervised learning
- When you complete a captcha and identify the images has cars, you’re labelling images!


